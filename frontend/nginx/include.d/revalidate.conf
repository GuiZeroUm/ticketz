# Public assets without a content hash may change at the same URL.
# Retain their bytes, but validate before reuse so branding updates stay fresh.
add_header Cache-Control "public, no-cache";
etag on;
if_modified_since exact;
